import { allRelics } from './relics';

export const INVALID_RELIC = allRelics.generateEmbed(
  "That relic doesn't exist",
  '__**Valid relics**__'
);
export const DATABASE_ERROR = 'Something went wrong. Please try again later.';
